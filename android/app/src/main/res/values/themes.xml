<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AssistantPeripheral" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/surface</item>
    </style>
    <!-- Transparent theme for VoiceShortcutActivity (trampoline — no visible UI) -->
    <style name="Theme.AssistantPeripheral.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
