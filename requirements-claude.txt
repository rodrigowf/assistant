# Claude Code harness — install this if you want to run sessions backed
# by the bundled `claude` CLI.  Skip it for a Qwen-only deployment.
#
# Loaded lazily by manager/__init__.py via PEP 562, so installing this
# does NOT change what's imported at backend startup — the SDK is only
# loaded the first time a Claude session is created.

# Floor bumped 2026-05-18 so we automatically pick up an upstream fix
# for claude-agent-sdk#378 (Query.close() can pin the event loop at
# 100% CPU via anyio's _deliver_cancellation busy-loop) if/when one
# ships.  Our manager/claude/session.py also monkey-patches Query.close
# defensively — the pin is just a safety net for new installs.
claude-agent-sdk>=0.1.29
